
// code that moves to a new question if the value of the question is null.  do this as a guard condition when setting the values for the squares.  e.g. if (tvQuestion.value === 100 || tvQuestion.value === 200) && tvQuestion.value !== null) 

// To show the answer along with a message about being right or wrong:
// if (input === tvQuestion.value){
    // append text node (answer) to answerBox id.
    // along with message that the answer was correct
// } else {
    //append text node (with answer) to answerBox id 
    // along with a message that the answer was incorrect 
// }